<h1><%= @artist.name %></h1>
<img src="<%= @artist.image_url %>" alt="">

<% @album.each do |item|%>
<div><%= item.name %></div>
<img src="<%= item.image_url %>" alt="">
<div><%= item.released_at %></div>

<% end %>
